import {articles} from '@/lib/content';
import {notFound} from 'next/navigation';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(a=>a.slug===slug);return {title:a?`${a.title} · Qonsul Insights`:'Artikel nicht gefunden',description:a?.description,openGraph:{title:a?.title,description:a?.description,type:'article',images:[]},twitter:{card:'summary',title:a?.title,description:a?.description,images:[]}};}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(a=>a.slug===slug);if(!a)notFound();return <><header className="header"><a className="brand" href="/"><img src="/qonsul-logo.png" alt="Qonsul"/></a><a className="button button-outline" href="/#insights">← Alle Insights</a></header><main className="article-page"><div className="eyebrow">{a.tag} · {a.time}</div><h1>{a.title}</h1><p className="lede">{a.description}</p>{a.sections.map(s=><section key={s.title}><h2>{s.title}</h2><p>{s.text}</p></section>)}<section><a className="source" href={a.source} target="_blank" rel="noreferrer">Weiterführende Primärquelle: {a.sourceLabel} ↗</a></section><div className="article-cta"><h2>Was bedeutet das für Ihr Team?</h2><p>Bringen Sie Ihr konkretes Problem mit. Quality Diagnostic hilft beim ersten Schritt.</p><a className="button button-green" href="/#analyse">Problem analysieren ↗</a></div></main></>;}
