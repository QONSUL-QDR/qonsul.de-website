import Brand from '@/app/brand';
import {services} from '@/lib/content';
import {notFound} from 'next/navigation';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(s=>s.slug===slug);return {title:s?`${s.title} · Qonsul`:'Leistung nicht gefunden',description:s?.lead,openGraph:{title:s?.title,description:s?.lead,images:[]},twitter:{card:'summary',title:s?.title,description:s?.lead,images:[]}};}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(s=>s.slug===slug);if(!s)notFound();return <><header className="header"><a className="brand" href="/"><Brand/></a><a className="button button-outline" href="/#leistungen">← Alle Leistungen</a></header><main className="article-page"><div className="eyebrow">QONSUL LEISTUNGEN</div><h1>{s.title}</h1><p className="lede">{s.lead}</p><section><h2>Gemeinsam Klarheit schaffen.</h2><p>{s.text}</p></section><section><h2>So unterstützen wir Ihr Team.</h2><ul>{s.items.map(i=><li key={i}>{i}</li>)}</ul></section><div className="article-cta"><h2>Der erste Schritt ist Ihr Problem.</h2><p>Starten Sie mit einer unverbindlichen Ursachenlandkarte. Danach entscheiden Sie, ob wir gemeinsam weiterarbeiten.</p><a className="button button-green" href="/#analyse">Quality Diagnostic starten ↗</a></div></main></>;}
